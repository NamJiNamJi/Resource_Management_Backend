<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.douzone.wehago.mapper.CarMapper">

    <insert id ="save" parameterType="com.douzone.wehago.dto.CarDTO">
        INSERT INTO tb_cars (car_seq, car_number, car_type, car_image, car_year, car_distance)
        values (#{carSeq}, #{carNumber}, #{carType}, #{carImage}, #{carYear}, #{carDistance});
    </insert>

    <select id ="findAll" resultType="com.douzone.wehago.dto.CarDTO">
        select * from tb_cars where car_seq = #{car_seq};
    </select>

    <select id = "findOne" resultType="com.douzone.wehago.dto.CarDTO">
        select car_seq, car_number, car_type, car_year, car_distance
        from tb_cars
        where car_seq = #{carSeq};
    </select>

    <update id="update" parameterType="com.douzone.wehago.dto.CarDTO">
        update tb_cars set car_seq = #{carSeq}, car_number = #{carNumber}, car_type = #{carType},
                           car_image = #{carImage}, car_year = #{carYear}, car_distance = #{carDistance};
    </update>

    <delete id="delete" parameterType="String">
        delete from tb_cars where car_seq = #{carSeq};
    </delete>

</mapper>